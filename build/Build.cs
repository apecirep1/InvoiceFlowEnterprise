using System;

namespace InvoiceFlow.Build;

public static class Build
{
    public static int Main(string[] args)
    {
        Console.WriteLine("Use `dotnet build InvoiceFlow.sln` or Docker Compose. This file is a NUKE/Bullseye-compatible placeholder entry point.");
        return 0;
    }
}
